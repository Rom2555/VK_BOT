# VK Bot — Бот для знакомств в ВКонтакте

Разработан в рамках учебного проекта.  
Подбирает кандидатов для знакомств на основе возраста, пола и города, а также позволяет сохранять понравившихся пользователей в избранное.

## Участники проекта

- **Лев Артемьев**
- **Роман Егоров**
- **Светлана Волкова**

## Структура проекта

Проект состоит из следующих файлов и папок:

- **database/** — модули работы с базой данных  
  - `__init__.py` — инициализация пакета  
  - `manager.py` — менеджер БД (работа с пользователями, кандидатами, избранным)  
  - `models.py` — модели SQLAlchemy (таблицы: users, candidates, favorites)

- **.env.example** — шаблон файла окружения  
- **.flake8** — настройки линтера flake8  
- **.gitignore** — файлы, игнорируемые Git  
- **config.py** — конфигурация (токены, настройки подключения к БД)  
- **keyboard.py** — генерация клавиатур для VK API  
- **main.py** — точка входа, запуск бота  
- **README.md** — этот файл  
- **requirements.txt** — зависимости проекта  
- **user_bot.py** — логика обработки сообщений от пользователя  
- **vk_searcher.py** — поиск пользователей, городов и фото через VK API
- **db_schema.png** — схема базы данных

## Схема базы данных

![Схема базы данных](db_schema.png)

## Как запустить

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Создайте файл `.env` на основе шаблона:
   ```bash
   cp .env.example .env
   ```

3. Заполните `.env` своими значениями:
   ```env
   GROUP_TOKEN=ваш_токен_группы_вк
   USER_TOKEN=ваш_токен_пользователя_вк
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=название_базы
   DB_USER=имя_пользователя
   DB_PASS=пароль
   ```

4. **Создайте базу данных в PostgreSQL**  
   Откройте pgAdmin или любой клиент PostgreSQL и выполните:
   ```sql
   CREATE DATABASE название_базы;
   ```
   (замените `название_базы` на то, что указали в `.env`)

   > 💡 Таблицы (`users`, `candidates`, `favorites`) создаются автоматически при первом запуске.

5. Запустите бота:
   ```bash
   python main.py
   ```

## Как работает

- Пользователь пишет `/start`
- Бот просит ввести возраст, выбрать пол и указать город
- Поиск идёт через VK API с фильтрами:
  - Возраст ±1 год от указанного
  - Пол: мужчины → женщинам, женщины → мужчинам
  - Город должен существовать в базе ВК
  - Только пользователи с открытым профилем и фото
- Каждого кандидата можно:
  - Пропустить (`Дальше`)
  - Добавить в избранное (`Добавить в избранное`)
  - Посмотреть список избранных (`Избранное`)
  - Начать новый поиск (`Новый поиск`)

> 💡 **Важно!**  
> - Кандидаты **не повторяются**: если человек уже в избранном — он не покажется снова  
> - Фото — **ТОП-3 по сумме лайков и комментариев**  
> - Бот устойчив к сбоям: при ошибках (например, потере интернета) он автоматически перезапускается

## Зависимости

- `vk-api==11.10.0` — работа с VK API  
- `python-dotenv==1.0.1` — загрузка переменных окружения  
- `SQLAlchemy==2.0.30` — ORM для работы с БД  
- `psycopg==3.3.2` — драйвер PostgreSQL
